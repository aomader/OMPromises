language: objective-c
before_install:
    - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
    - cd Tests/OMPromisesTests && pod install
script: cd Tests/OMPromisesTests && ./run_tests.sh
