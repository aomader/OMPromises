language: objective-c
osx_image: xcode7.2

before_script:
  - brew update
  - brew upgrade xctool || true
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
  - cd $TRAVIS_BUILD_DIR/Project && pod install

script: cd $TRAVIS_BUILD_DIR/Project && ./run_tests.sh

notifications:
  email: false
